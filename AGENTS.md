# AGENT 协作约定
本文档适用于整个仓库。修改子目录前，应同时遵守该子目录中更具体的 `AGENTS.md`；若规则冲突，以路径更深的文件为准。

## 核心原则

- 不能假设人类协作人员都是专业的，先研究再给出建议方案。
- 不能MVP的思考虑方式，需要使用 **架构** 思维，实现可以最小化不扩展。
- 非必要不要进行抽象。
- 所有文档中：表达、流程、关系、顺序的使用 `mermaid` 语法。
- 与人员沟通时不要使用 `mermaid`。CLI下无法展示

## 语言约定

- Rust 标识符、模块名、文件名、crate 名和协议字段名使用英文。
- 代码注释、Rust 文档注释、README、设计文档、ADR、测试说明以及面向开发者的
  OpenAPI 文档使用中文。
- 面向最终用户或外部调用方的运行时输出使用英文，包括 API 错误消息、CLI 输出、
  UI 文案以及可能直接暴露给用户的 `Display` 文本。
- 助手向用户汇报进度、说明结果或提出问题时使用中文。
- 行业标准名称、Rust 类型名和协议术语可以保留英文，不强制生硬翻译。

## 文档约定


## AGENT协作工作流

1. 在 `.agent/proposals/` 下编写本次协作内容的提案, 包括不限于架构、工作组织、代码示例、流程、API等。
2. 在过程中只修改提案并由人类最终确认提案。
3. 提案人类确认后，将提案转为PLAN, 并记录在 `.agent/plan.md` 下， 原PLAN未完成则由人类决策。
4. 开始执行PLAN则调度独立的子AGENT负责，主AGENT负责审查、报告、并等待人类确认Commit。
5. 可共享的上下文信息写入`.agent/CONTEXT.md`。并看任务时机要重新读取

## Rust 实现约定

- 遵守 workspace 的 Rust 2024 edition 和 lint 配置。
- 禁止新增 `unsafe`；workspace 已设置 `unsafe_code = "forbid"`。
- 优先使用类型系统表达不变量，避免依赖调用者记忆约定。
- 公共类型、trait、方法和错误应提供中文 Rust 文档注释。
- Rust 文档注释 `///` 和 `//!` 使用 rustdoc Markdown；类型、函数、字段、trait 等
  公共文档按需使用反引号、链接、列表、代码块以及 `# Errors`、`# Panics`、
  `# Examples` 等标准章节。
- 普通实现注释 `//` 使用简洁中文纯文本，不强制使用 Markdown，也不使用无意义的复杂
  标题、表格或排版。
- OpenAPI 描述可以使用基础 Markdown，但不得依赖特定渲染器的私有扩展语法。
- 代码必须包含必要的中文关键注释，说明重要设计意图、核心不变量、模块边界、复杂算法、
  非直观分支以及关键边界条件。注释应解释“为什么这样实现”，不得仅逐行复述代码行为；
  简单且自解释的代码无需添加冗余注释。
- 错误类型保留可匹配的结构化信息；不要仅使用无法分类的字符串错误。
- 导出宏使用绝对标准库路径或 `$crate` 路径，避免依赖调用位置的 `use`。
- 宏必须至少有一个真实下游集成测试；非法输入适合使用 compile-fail doctest。
- 不为尚未出现的需求增加持久化、异步、动态分派、Serde 或 Schema 约束。
- 修改现有代码时保持改动范围聚焦，不顺手重写无关模块。
- 不使用 `mod.rs` 而采用最新的 `file.rs` 规范。


## 验证要求

完成 Rust 修改后，根据影响范围至少执行：

```bash
cargo fmt --all -- --check
cargo test -p <package>
cargo clippy -p <package> --all-targets -- -D warnings
```

影响公共宏、跨 crate API、workspace manifest 或依赖关系时，再执行：

```bash
cargo test --workspace
cargo metadata --no-deps --format-version 1
```
如果 workspace 级检查被无关的既有告警阻塞，应单独验证修改涉及的 package，并在结果
中明确指出阻塞文件和告警，不得将其描述为本次检查已全部通过。

## Git 工作流

项目当前处于早期阶段，不使用迭代分支工作流。所有开发均在本地当前的 `main` 分支上
进行，不创建 `develop`、`feature/*` 或其他开发分支。

具体规则：

1. 修改代码、文档或配置时直接保留在当前工作区，不因任务类型切换或创建分支。
2. 未经用户明确要求，不创建 Git 提交；需要提交时，提交主题必须使用有意义的中文并
   准确概括改动。可以保留 `feat(engine):`、`fix(common):` 等 Conventional Commit
   前缀，但冒号后的主题必须使用中文；禁止使用 `update`、`changes`、`wip` 等无法说明
   意图的笼统提交信息。
3. 不执行 `git push`、远程分支操作、tag、release 或 PR 操作。
4. 不使用 `git branch -D`、`git reset --hard`、强制推送或其他可能丢失工作的命令，
   除非用户明确授权并且目标已经核实。
5. 工作区存在用户未提交修改时，必须保留这些修改；不得覆盖、回退或混入无关变更。
6. 项目进入需要多人并行开发或版本发布的阶段后，再单独设计并启用迭代工作流。

## 完成标准

- 实现与请求范围一致，没有引入未经确认的领域假设。
- 模块依赖方向符合本文件约定。
- 文档与注释为中文，对外运行时输出为英文。
- 相关格式化、测试和 lint 已执行并通过，或已准确报告既有阻塞。
- 未经用户明确要求不执行 Git 提交，且不改变当前 `main` 分支。
